import cv2
cap = cv2.VideoCapture('video.mp4')
frames=[]
while True:
    ret,f = cap.read()
    if not ret: break
    frames.append(f)
for f in frames[::-1]:
    cv2.imshow("Slow Reverse",f)
    if cv2.waitKey(100)==27: break
