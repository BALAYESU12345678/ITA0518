import cv2

img = cv2.imread("image.jpg")
if img is None:
    exit()

result = cv2.rotate(img, cv2.ROTATE_90_COUNTERCLOCKWISE)

cv2.imshow("Rotate 270", result)
cv2.waitKey(0)
cv2.destroyAllWindows()
